<!--
앱의 업데이트 카드에 그대로 뜨는 글이다. 릴리스 워크플로가 `# <버전>` 섹션을 찾아 GitHub
릴리스 본문 맨 앞에 넣는다 (자동 생성 변경로그는 그 뒤에 붙는다).

쓰는 법:
- **읽는 사람은 개발자가 아니다.** 로그·폴러·데몬·오프셋 같은 말은 쓰지 않는다. 코드가 뭘 했는지가
  아니라 **화면에서 뭐가 달라지는지**를 쓴다. 한 줄 쓰고 "진단 화면을 한 번도 안 열어본 사람이
  이걸 이해할까?" 를 되물어볼 것.
- `- ` 목록만 쓴다. 카드의 마크다운 처리는 `**굵게**` 하나뿐이라 `#` 나 `*` 는 기호 그대로 보인다.
- 카드는 릴리스 하나만 보여준다. 그래서 **직전 버전을 건너뛴 사람 기준**으로 쓴다 — 같은 문제를
  나눠 고친 연속 릴리스라면 마지막 섹션에 둘 다 적는 게 맞다 (0.6.5 가 그 예다).
- 새 버전을 낼 때 이 파일에 섹션을 먼저 추가한다 (RELEASING.md 1번 단계). 빠뜨리면 릴리스는
  그대로 나가지만 카드에는 자동 변경로그 링크만 뜬다.
-->

# 0.6.7

- 앱을 껐다가 **3일 안에** 다시 켜면 그대로 이어집니다. 예전에는 앱을 끄고 1시간만 지나도 연결이 풀려서, 게임할 때마다 개발자 옵션에 들어가 무선 디버깅을 다시 켜야 했습니다.
- 무선 디버깅이 꺼지는 경우를 안내에 제대로 적었습니다 — 재부팅뿐 아니라 WiFi가 잠깐 끊기거나 오래 안 쓰면 Android가 알아서 끕니다. 앱 문제가 아닙니다.

# 0.6.6

- 이 카드에 바뀐 내용이 쉬운 말로 나옵니다. 예전에는 주소 한 줄만 떠서 뭐가 달라졌는지 알 수 없었습니다.
- 설명이 길어도 중간에 잘리지 않고 끝까지 보입니다.

# 0.6.5

- 게임이 꺼지거나 튕기면 패널의 시간도 같이 멈춥니다. 예전에는 게임이 없는데도 시간만 계속 올라가서, 자리를 비운 만큼 시간당 수익이 실제보다 낮게 나왔습니다.
- 게임이 멈춘 동안에는 패널에 **게임 꺼짐** 이라고 표시되고, 게임을 다시 켜면 알아서 이어서 셉니다.
- 앱이 게임 기록을 읽지 못하게 되면 패널에 **끊김** 이라고 바로 알려줍니다. 예전에는 화면상 멀쩡해 보이는데 수익만 안 오르는 상태가 됐습니다.
- 그 상태가 되면 앱을 직접 열지 않아도 스스로 다시 연결을 시도합니다. 예전에는 앱을 한 번 열었다 나와야 다시 붙었습니다.

# 0.6.4

- 앱이 게임 기록을 읽지 못하게 되면 패널에 **끊김** 이라고 표시하고, 스스로 다시 연결을 시도합니다.
- 무선 디버깅을 다시 켠 뒤에도 "무선 디버깅을 켜주세요" 안내가 사라지지 않던 문제를 고쳤습니다.

# 0.6.3

- 지난 버전들의 설치 파일을 계속 받을 수 있도록 정리했습니다.
- Android 10 을 쓰는 분들을 위한 마지막 버전(0.5.16) 안내를 추가했습니다.
